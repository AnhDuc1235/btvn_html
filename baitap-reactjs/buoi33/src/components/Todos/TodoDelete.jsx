// import { useState } from 'react';
// import { TodoContext } from '../../TodoReducer';
// import { use } from 'react';

// export default function TodoDelete() {
//     // const [title, setTitle] = useState("")
//     const { dispatch } = use(TodoContext)
//   const handleDelete = (index) => {
//     setData(state..filter((item, _index) => _index !== index));

//   };

//   return (
//     <form onSubmit={handleAdd}>
//         <input type="text" placeholder='Title...' onChange={(e) => setTitle(e.target.validationMessage)}/>
//         <button>add</button>
//     </form>
    
//   )
// }
